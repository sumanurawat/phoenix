# Unified Draft-First Workflow - Visual Guide

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐           ┌─────────────────────────┐        │
│  │ Create Page  │           │   Profile (Drafts Tab)  │        │
│  │              │           │                         │        │
│  │ [Generate]   │──────────▶│  ┌─────────────────┐   │        │
│  │   Button     │ redirect  │  │ Pending Card    │   │        │
│  │              │           │  │ ╔═══════════╗   │   │        │
│  └──────────────┘           │  │ ║ shimmer   ║   │   │        │
│                             │  │ ╚═══════════╝   │   │        │
│                             │  └─────────────────┘   │        │
│                             │                         │        │
│                             │  After ~15-120 seconds: │        │
│                             │  ┌─────────────────┐   │        │
│                             │  │ Draft Card      │   │        │
│                             │  │ ┌─────────────┐ │   │        │
│                             │  │ │ [Preview]   │ │   │        │
│                             │  │ └─────────────┘ │   │        │
│                             │  │ [Publish] [Del] │   │        │
│                             │  └─────────────────┘   │        │
│                             └─────────────────────────┘        │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    │ POST /api/generate/creation
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                         BACKEND API                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  api/generation_routes.py                                      │
│  ┌────────────────────────────────────────────────┐            │
│  │ POST /api/generate/creation                    │            │
│  │  1. Validate input (type, prompt)              │            │
│  │  2. Call CreationService.create_pending()      │            │
│  │  3. Enqueue worker task (image or video)       │            │
│  │  4. Return 202 Accepted                        │            │
│  └────────────────────────────────────────────────┘            │
│                      │                   │                      │
└──────────────────────┼───────────────────┼──────────────────────┘
                       │                   │
                       ▼                   ▼
┌──────────────────────────────────────────────────────────────────┐
│                     CREATION SERVICE                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  services/creation_service.py                                   │
│  ┌─────────────────────────────────────────────────┐            │
│  │ create_pending_creation()                       │            │
│  │                                                 │            │
│  │  [Firestore Transaction START]                 │            │
│  │    1. Get user document                        │            │
│  │    2. Check token balance >= cost              │            │
│  │    3. Debit tokens (balance -= cost)           │            │
│  │    4. Create draft (status: pending)           │            │
│  │    5. Log transaction (type: debit)            │            │
│  │  [Firestore Transaction COMMIT]                │            │
│  │                                                 │            │
│  │  Returns: creation_id                          │            │
│  └─────────────────────────────────────────────────┘            │
│                                                                  │
│  ┌─────────────────────────────────────────────────┐            │
│  │ handle_generation_failure()                     │            │
│  │                                                 │            │
│  │  [Firestore Transaction START]                 │            │
│  │    1. Get creation document                    │            │
│  │    2. Check if already refunded (idempotent)   │            │
│  │    3. Add tokens back (balance += cost)        │            │
│  │    4. Mark creation as refunded                │            │
│  │    5. Log transaction (type: refund)           │            │
│  │  [Firestore Transaction COMMIT]                │            │
│  │                                                 │            │
│  └─────────────────────────────────────────────────┘            │
└──────────────────────────────────────────────────────────────────┘
                       │                   │
                       ▼                   ▼
┌──────────────────────────────────────────────────────────────────┐
│                     CELERY WORKERS                               │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  jobs/async_image_generation_worker.py                          │
│  ┌─────────────────────────────────────────────────┐            │
│  │ @celery_app.task                                │            │
│  │ generate_image_task(creation_id, user_id, ...)  │            │
│  │                                                 │            │
│  │  try:                                           │            │
│  │    1. Update status → processing               │            │
│  │    2. Call ImageGenerationService              │            │
│  │    3. Upload to R2 storage                     │            │
│  │    4. Update status → draft (+ media_url)      │            │
│  │                                                 │            │
│  │  except SafetyFilterError:                     │            │
│  │    creation_service.handle_failure()           │            │
│  │    (automatic refund)                          │            │
│  └─────────────────────────────────────────────────┘            │
│                                                                  │
│  jobs/async_video_generation_worker.py                          │
│  ┌─────────────────────────────────────────────────┐            │
│  │ @celery_app.task                                │            │
│  │ generate_video_task(creation_id, user_id, ...)  │            │
│  │                                                 │            │
│  │  try:                                           │            │
│  │    1. Update status → processing               │            │
│  │    2. Call VeoVideoGenerationService           │            │
│  │    3. Upload to R2 storage                     │            │
│  │    4. Update status → draft (+ media_url)      │            │
│  │                                                 │            │
│  │  except PolicyViolationError:                  │            │
│  │    creation_service.handle_failure()           │            │
│  │    (automatic refund)                          │            │
│  └─────────────────────────────────────────────────┘            │
└──────────────────────────────────────────────────────────────────┘
                       │                   │
                       ▼                   ▼
┌──────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Google Imagen│  │ Google Veo   │  │ Cloudflare   │          │
│  │              │  │              │  │ R2 Storage   │          │
│  │ (Images)     │  │ (Videos)     │  │              │          │
│  │ ~10-20s      │  │ ~60-120s     │  │ (Public URLs)│          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└──────────────────────────────────────────────────────────────────┘
```

---

## State Transition Diagram

```
┌────────────┐
│   START    │
│ User clicks│
│ "Generate" │
└─────┬──────┘
      │
      │ POST /api/generate/creation
      │
      ▼
┌──────────────────────────────┐
│  PENDING                     │ ◄─── User sees this immediately
│  • Blue badge                │      (< 1 second after click)
│  • Shimmer animation         │
│  • "Refresh" button          │
│  • Tokens debited            │
└─────┬────────────────────────┘
      │
      │ Worker picks up task
      │
      ▼
┌──────────────────────────────┐
│  PROCESSING                  │ ◄─── User sees after refresh
│  • Orange badge              │      (~5-10 seconds later)
│  • Shimmer continues         │
│  • "Refresh" button          │
└─────┬────────────────────────┘
      │
      │ Success or Failure?
      │
      ├─────────────────┬─────────────────┐
      │ SUCCESS         │ FAILURE         │
      ▼                 ▼                 ▼
┌────────────────┐  ┌──────────────────────────────┐
│  DRAFT         │  │  FAILED                      │
│  • Green badge │  │  • Red badge + background    │
│  • Media       │  │  • Error message             │
│    preview     │  │  • "Delete" button           │
│  • "Publish"   │  │  • Tokens refunded           │
│  • "Delete"    │  │    (automatic)               │
└────┬───────────┘  └──────────────────────────────┘
      │
      │ User clicks "Publish"
      │
      ▼
┌────────────────┐
│  PUBLISHED     │
│  • Appears in  │
│    Posts tab   │
│  • Visible in  │
│    Explore     │
└────────────────┘
```

---

## Token Transaction Flow

### Image Generation (1 Token)

```
Time  │ Action                        │ Balance │ Transaction Log
──────┼───────────────────────────────┼─────────┼─────────────────────
 0s   │ Initial balance               │  100    │ -
 0.1s │ [Transaction] Debit 1 token   │   99    │ debit: -1 (pending creation)
15s   │ Worker completes              │   99    │ -
──────┼───────────────────────────────┼─────────┼─────────────────────
      │ Net: -1 token                 │   99    │ ✓ 1 debit entry
```

### Video Generation Failure (10 Tokens)

```
Time  │ Action                        │ Balance │ Transaction Log
──────┼───────────────────────────────┼─────────┼─────────────────────
 0s   │ Initial balance               │  100    │ -
 0.1s │ [Transaction] Debit 10 tokens │   90    │ debit: -10 (pending)
45s   │ Worker fails (policy error)   │   90    │ -
45.2s │ [Transaction] Refund 10 tokens│  100    │ refund: +10 (linked to debit)
──────┼───────────────────────────────┼─────────┼─────────────────────
      │ Net: 0 tokens                 │  100    │ ✓ 1 debit + 1 refund
```

### Retry Scenario (Idempotent Refund)

```
Time  │ Action                        │ Balance │ Transaction Log
──────┼───────────────────────────────┼─────────┼─────────────────────
 0s   │ Initial balance               │  100    │ -
 0.1s │ [Transaction] Debit 10 tokens │   90    │ debit: -10
30s   │ Worker attempt 1 fails        │   90    │ -
30.2s │ [Transaction] Refund 10 tokens│  100    │ refund: +10
31s   │ Celery auto-retry (attempt 2) │  100    │ -
31.1s │ Check: already refunded=true  │  100    │ -
31.2s │ Skip duplicate refund         │  100    │ (no new entry)
──────┼───────────────────────────────┼─────────┼─────────────────────
      │ Net: 0 tokens                 │  100    │ ✓ 1 debit + 1 refund (no dupe)
```

---

## UI State Examples

### Pending Card (Immediate)
```
┌────────────────────────────────────┐
│ 🖼️ IMAGE                          │
│ ┌────────────────────────────────┐ │
│ │ ╔════════════════════════════╗ │ │
│ │ ║  shimmer animation here    ║ │ │ ◄── Loading skeleton
│ │ ╚════════════════════════════╝ │ │
│ └────────────────────────────────┘ │
│ Prompt: "A beautiful sunset..."   │
│ Status: [Pending] (blue)           │
│ [Refresh]                          │
└────────────────────────────────────┘
```

### Processing Card (After 5-10s)
```
┌────────────────────────────────────┐
│ 🎬 VIDEO                           │
│ ┌────────────────────────────────┐ │
│ │ ╔════════════════════════════╗ │ │
│ │ ║  shimmer animation here    ║ │ │ ◄── Still loading
│ │ ╚════════════════════════════╝ │ │
│ └────────────────────────────────┘ │
│ Prompt: "A cat playing..."         │
│ Status: [Processing] (orange)      │
│ [Refresh]                          │
└────────────────────────────────────┘
```

### Draft Card (Completed)
```
┌────────────────────────────────────┐
│ 🖼️ IMAGE                          │
│ ┌────────────────────────────────┐ │
│ │                                │ │
│ │   [Beautiful sunset image]     │ │ ◄── Actual image preview
│ │        (clickable)             │ │
│ │                                │ │
│ └────────────────────────────────┘ │
│ Prompt: "A beautiful sunset..."   │
│ Status: [Draft] (green)            │
│ [Publish] [Delete]                 │
└────────────────────────────────────┘
```

### Failed Card (Error)
```
┌────────────────────────────────────┐ ◄── Red background
│ 🎬 VIDEO                           │
│ ⚠️ Generation Failed                │
│ Error: Content violates policy     │
│                                    │
│ Prompt: "Inappropriate content..." │
│ Status: [Failed] (red)             │
│ [Delete]                           │
│ ✓ Tokens refunded                  │
└────────────────────────────────────┘
```

---

## Code Interaction Map

```
templates/create.html (generateButton)
    │
    │ fetch('/api/generate/creation', ...)
    │
    ▼
api/generation_routes.py
    │
    │ creation_service.create_pending_creation(...)
    │
    ▼
services/creation_service.py
    │
    ├─▶ token_service.get_user_balance(user_id)
    ├─▶ [Firestore Transaction]
    │     ├─ user_ref.update({'tokens': new_balance})
    │     ├─ creation_ref.set({status: 'pending', ...})
    │     └─ transaction_ref.set({type: 'debit', ...})
    │
    └─▶ Returns creation_id
         │
         ▼
api/generation_routes.py
    │
    │ celery_app.send_task('generate_image_task', ...)
    │ OR
    │ celery_app.send_task('generate_video_task', ...)
    │
    ▼
jobs/async_image_generation_worker.py
OR
jobs/async_video_generation_worker.py
    │
    ├─▶ creation_service.update_status('processing')
    ├─▶ image_service.generate_image(prompt, ...)
    │   OR
    │   veo_service.generate_video(prompt, ...)
    ├─▶ r2_client.upload(media_data)
    └─▶ creation_service.update_status('draft', media_url=url)

    [On Error]
    └─▶ creation_service.handle_generation_failure(...)
         │
         ├─▶ [Firestore Transaction]
         │     ├─ Check if refunded == true (skip if yes)
         │     ├─ user_ref.update({'tokens': balance + cost})
         │     ├─ creation_ref.update({refunded: true, status: 'failed'})
         │     └─ transaction_ref.set({type: 'refund', linked_transaction: debit_id})
         │
         └─▶ Returns success
```

---

## Deployment Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                      CLOUD ENVIRONMENT                         │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────┐         ┌──────────────────┐          │
│  │  Cloud Run       │         │  Cloud Run       │          │
│  │  (Flask App)     │         │  (Celery Worker) │          │
│  │                  │         │                  │          │
│  │  - API routes    │◄────────┤  - Image task    │          │
│  │  - Templates     │  Redis  │  - Video task    │          │
│  │  - Static files  │  queue  │                  │          │
│  │                  │         │                  │          │
│  │  Max 1 instance  │         │  Autoscale 0-10  │          │
│  └──────┬───────────┘         └───────┬──────────┘          │
│         │                              │                     │
│         │                              │                     │
│         ▼                              ▼                     │
│  ┌─────────────────────────────────────────────┐            │
│  │  Redis (Cloud Memorystore)                  │            │
│  │  - Task queue                               │            │
│  │  - Session storage (future)                 │            │
│  └─────────────────────────────────────────────┘            │
│                                                               │
│         ▼                              ▼                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Firestore    │  │ R2 Storage   │  │ Secret Mgr   │      │
│  │              │  │              │  │              │      │
│  │ - Users      │  │ - Images     │  │ - API keys   │      │
│  │ - Creations  │  │ - Videos     │  │ - Credentials│      │
│  │ - Tokens     │  │              │  │              │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└───────────────────────────────────────────────────────────────┘
```

---

## Performance Benchmarks

| Metric | Target | Typical |
|--------|--------|---------|
| API Response (202) | < 500ms | ~200ms |
| Redirect to drafts | < 1s | ~500ms |
| Image generation | < 30s | 10-20s |
| Video generation | < 3min | 60-120s |
| Token refund | < 2s | ~1s |
| Celery task pickup | < 5s | ~2s |

---

## Monitoring Dashboard (Future)

```
┌────────────────────────────────────────────────────────────┐
│  UNIFIED WORKFLOW DASHBOARD                                │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Today's Metrics                                           │
│  ┌──────────┬──────────┬──────────┬──────────┐           │
│  │ Total    │ Success  │ Failed   │ Pending  │           │
│  │ 1,234    │ 1,150    │ 42       │ 42       │           │
│  │ (100%)   │ (93.2%)  │ (3.4%)   │ (3.4%)   │           │
│  └──────────┴──────────┴──────────┴──────────┘           │
│                                                            │
│  Average Processing Time                                   │
│  ┌──────────────────────────────────────┐                │
│  │ Images:  █████░░░░░ 15.3s            │                │
│  │ Videos:  ███████████████░░ 87.2s     │                │
│  └──────────────────────────────────────┘                │
│                                                            │
│  Token Refund Rate: 100% (42/42)                          │
│  Queue Depth: 8 tasks                                     │
│  Worker Utilization: 65%                                  │
│                                                            │
│  Recent Failures:                                          │
│  • 14:32 - PolicyViolationError (user_123) ✓ Refunded    │
│  • 14:28 - SafetyFilterError (user_456) ✓ Refunded       │
│  • 14:15 - TimeoutError (user_789) ✓ Refunded            │
└────────────────────────────────────────────────────────────┘
```

---

## Error Recovery Flowchart

```
            ┌─────────────────┐
            │ Task Fails      │
            └────────┬────────┘
                     │
                     ▼
            ┌─────────────────┐
            │ Is refunded?    │
            └────────┬────────┘
                     │
            ┌────────┴────────┐
            │                 │
           YES               NO
            │                 │
            ▼                 ▼
    ┌──────────────┐  ┌──────────────────┐
    │ Skip refund  │  │ Start transaction│
    │ (idempotent) │  │ to refund tokens │
    └──────────────┘  └────────┬─────────┘
                               │
                               ▼
                      ┌─────────────────┐
                      │ Transaction OK? │
                      └────────┬────────┘
                               │
                      ┌────────┴────────┐
                      │                 │
                     YES               NO
                      │                 │
                      ▼                 ▼
              ┌──────────────┐  ┌──────────────┐
              │ Mark refunded│  │ Log error    │
              │ status=failed│  │ Retry later  │
              └──────────────┘  └──────────────┘
                      │                 │
                      └────────┬────────┘
                               │
                               ▼
                      ┌─────────────────┐
                      │ Notify user     │
                      │ (UI shows error)│
                      └─────────────────┘
```

---

## Local vs Production Differences

| Aspect | Local | Production |
|--------|-------|------------|
| Redis | localhost:6379 | Cloud Memorystore |
| Workers | 1 process | 0-10 autoscale |
| Session storage | ./flask_session/ | Redis (future) |
| Logs | stdout | Cloud Logging |
| Secrets | .env file | Secret Manager |
| Images/Videos | R2 (same) | R2 (same) |
| Firestore | Same database | Same database |

---

This visual guide complements the testing and implementation documents. Use it to:
- Understand system flow at a glance
- Debug issues by following state transitions
- Onboard new team members quickly
- Plan future enhancements

**Next**: Run through `QUICK_START_UNIFIED_WORKFLOW.md` for hands-on testing!
